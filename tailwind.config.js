/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      "./app/**/*.{js,ts,jsx,tsx}"
      // other paths if needed
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  }